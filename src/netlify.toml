
[build.environment]
  NODE_VERSION = "16"
  NPM_FLAGS = "--legacy-peer-deps"
[build]
  command = "npm install --legacy-peer-deps && npm run build"
  publish = "build"  # Replace with your output folder
  command = "npm run build"


[[redirects]]
from="/*"
to="index.html"
status=200
